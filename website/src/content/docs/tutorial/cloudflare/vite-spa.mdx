---
title: Add a Vite SPA
description: Ship a Vite single-page app from the same Stack as your Worker — built, bundled, and deployed to Cloudflare in one command.
sidebar:
  order: 3
---

import { Code, Tabs, TabItem } from "@astrojs/starlight/components";

You have a Worker, a Durable Object, and a chat `Room` that
broadcasts WebSocket messages — enough backend for an interesting
app, but no UI. In this part you'll add a Vite single-page app
that builds and deploys from the same `alchemy.run.ts` as your
Worker, with no separate build pipeline or Wrangler config.

`Cloudflare.Vite` runs `vite build` for you, ships the assets to
Cloudflare, and serves them through a Worker — so your frontend and
backend share one URL surface and one deploy.

## Scaffold a Vite app

Create a Vite project in a `web/` subfolder so it lives next to your
existing `alchemy.run.ts` and `src/worker.ts`:

<Tabs syncKey="pkgManager">
  <TabItem label="bun" icon="bun">
    <Code code="bun create vite@latest web -- --template vanilla-ts" lang="sh" />
  </TabItem>
  <TabItem label="npm" icon="npm">
    <Code code="npm create vite@latest web -- --template vanilla-ts" lang="sh" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code="pnpm create vite@latest web --template vanilla-ts" lang="sh" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code="yarn create vite web --template vanilla-ts" lang="sh" />
  </TabItem>
</Tabs>

Install its deps:

<Tabs syncKey="pkgManager">
  <TabItem label="bun" icon="bun">
    <Code code="cd web && bun install && cd .." lang="sh" />
  </TabItem>
  <TabItem label="npm" icon="npm">
    <Code code="cd web && npm install && cd .." lang="sh" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code="cd web && pnpm install && cd .." lang="sh" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code="cd web && yarn && cd .." lang="sh" />
  </TabItem>
</Tabs>

You should now have a `web/index.html`, `web/src/main.ts`,
`web/vite.config.ts`, etc. Confirm it builds standalone:

```sh
cd web && bun run build && cd ..
```

## Add `Cloudflare.Vite` to the Stack

`Cloudflare.Vite` is a thin wrapper over `Cloudflare.Worker` that
runs `vite build` and ships the resulting assets. Point `rootDir`
at your Vite project; configure asset handling for SPA fallback
(every unmatched route serves `index.html`):

```diff lang="typescript"
// alchemy.run.ts
import * as Alchemy from "alchemy";
import * as Cloudflare from "alchemy/Cloudflare";
import * as Effect from "effect/Effect";
import Worker from "./src/worker.ts";

+const Web = Cloudflare.Vite("Web", {
+  rootDir: "./web",
+  compatibility: { flags: ["nodejs_compat"] },
+  assets: {
+    config: {
+      htmlHandling: "auto-trailing-slash",
+      notFoundHandling: "single-page-application",
+    },
+  },
+});

export default Alchemy.Stack(
  "MyApp",
  {
    providers: Cloudflare.providers(),
    state: Cloudflare.state(),
  },
  Effect.gen(function* () {
    const worker = yield* Worker;
+    const web = yield* Web;
    return {
      url: worker.url,
+      webUrl: web.url,
    };
  }),
);
```

The `notFoundHandling: "single-page-application"` setting is what
makes client-side routing work — a deep link like `/about` returns
`index.html` instead of a 404, and your router handles it on the
client.

## Deploy

```sh
bun alchemy deploy
```

Alchemy runs `vite build` in `web/`, uploads the assets, creates a
Worker that serves them, and prints the new `webUrl` stack output:

```
{
  url: "https://myapp-worker-dev-you.workers.dev",
  webUrl: "https://myapp-web-dev-you.workers.dev",
}
```

## Verify

Hit it with `curl`:

```sh
curl -s https://myapp-web-dev-you.workers.dev | head -5
# <!doctype html>
# <html lang="en">
#   <head>
#     <meta charset="UTF-8" />
#     ...
```

Or add an integration test alongside the existing ones:

```diff lang="typescript"
// test/integ.test.ts
import { beforeAll, deploy, expect, test } from "alchemy/Test/Bun";
import * as Effect from "effect/Effect";
import * as HttpClient from "effect/unstable/http/HttpClient";
import Stack from "../alchemy.run.ts";

const stack = beforeAll(deploy(Stack));

+test(
+  "Web SPA serves index.html",
+  Effect.gen(function* () {
+    const { webUrl } = yield* stack;
+
+    const response = yield* HttpClient.get(webUrl);
+    expect(response.status).toBe(200);
+    expect(yield* response.text).toContain("<!doctype html>");
+  }),
+);
```

## Local dev

`alchemy dev` (from [Tutorial Part 4](/tutorial/part-4)) runs Vite,
your Worker, and any Durable Objects / Containers / Workflows you
added — all under one watcher. Edit a `.ts` in `web/src/` and the
browser hot-reloads against the live Cloudflare resources you
deployed earlier.

```sh
alchemy dev
```

## Use a different framework

`Cloudflare.Vite` works with any Vite-based framework. The example
above uses vanilla TypeScript; here are the most common choices and
the example repo to crib from for each:

- **TanStack Start** — full-stack React with file-based routing and
  server functions. Needs `compatibility.flags: ["nodejs_compat"]`.
  See [examples/cloudflare-tanstack](https://github.com/alchemy-run/alchemy-effect/tree/main/examples/cloudflare-tanstack).
- **SolidStart** — SSR Solid with file-based routing. See
  [examples/cloudflare-solidstart](https://github.com/alchemy-run/alchemy-effect/tree/main/examples/cloudflare-solidstart).
- **SolidJS SSR (manual)** — when you want full control over the
  SSR pipeline rather than SolidStart's conventions. See
  [examples/cloudflare-solidjs-ssr](https://github.com/alchemy-run/alchemy-effect/tree/main/examples/cloudflare-solidjs-ssr).
- **Vue 3 SPA** — Vite's default Vue template, with the same
  `notFoundHandling: "single-page-application"` config as above.
  See [examples/cloudflare-vue](https://github.com/alchemy-run/alchemy-effect/tree/main/examples/cloudflare-vue).
- **Plain static site** — a single `index.html` (no framework)
  ships through `Cloudflare.Vite("Website")` with no extra config.
  See [examples/cloudflare-static-site](https://github.com/alchemy-run/alchemy-effect/tree/main/examples/cloudflare-static-site).

For deeper coverage of framework-specific patterns (asset configs,
SSR vs SPA tradeoffs, custom build steps), see the
[Frontend frameworks guide](/guides/frontends).

Your app now ships a Worker backend and a Vite frontend from the
same `alchemy.run.ts`, deploying together with one command. Next
you'll [run a Container](/tutorial/cloudflare/containers) so each
Durable Object instance has its own long-lived process for
executing untrusted code or running binaries.
