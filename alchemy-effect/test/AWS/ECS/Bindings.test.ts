import { describe, test } from "vitest";

describe("ECS Bindings", () => {
  describe("RunTask", () => {
    test.skip("runs a bound task on a cluster", () => {});
  });

  describe("DescribeTasks", () => {
    test.skip("describes tasks in a bound cluster", () => {});
  });

  describe("ListTasks", () => {
    test.skip("lists tasks in a bound cluster", () => {});
  });

  describe("StopTask", () => {
    test.skip("stops a task in a bound cluster", () => {});
  });
});
